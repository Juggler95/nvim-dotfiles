# Setup

#### I use Arch Linux but this should mostly work the same on macos

#### First backup your neovim config
```bash
mv ~/.config/nvim ~/.config/nvim.bak
```


#### Clone the repo
```bash
git clone https://github.com/Juggler95/nvim.git ~/.config/nvim
```
